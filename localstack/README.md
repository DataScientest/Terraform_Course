# LocalStack

Dans cette première branche, LocalStack sert à simuler localement des services AWS-like.

Pour l'instant, seul `S3` est activé dans `docker-compose.yml`.

Le bucket d'artefacts sera créé plus tard par Terraform.
